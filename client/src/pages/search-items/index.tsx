import { Card, CardContent } from "@components/card";
import { Text } from "@components/text";

export const SearchItems = () => {
  return (
    <Card>
      <CardContent>
        <Text>A página completa de procura será implementada em uma próxima etapa.</Text>
      </CardContent>
    </Card>
  );
};
